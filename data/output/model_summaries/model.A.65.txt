install the requirements sudo pip install r requirements.txt usage by default the tool searches on all search engines you can set this up by arguments but you need to put the credentials as stated above.