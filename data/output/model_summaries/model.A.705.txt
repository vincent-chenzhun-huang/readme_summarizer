if you do not wish to run the tests and only want to build the application use build the frontend with npm open a terminal in the ui directory location of package.json run to install the dependencies build the project with the following command this will place the compiled output into the and will be bundled in the next backend build running the application running the application locally can be done with either an h2 inmemory database or with a docker container of mysql.