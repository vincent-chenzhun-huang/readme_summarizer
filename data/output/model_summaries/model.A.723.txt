note on logs format if log entry is in json it will be formatted according to its values types i.e. each field will be stored in corresponding table stringfields numberfields booleanfields nullfields or labels the last one is used for containers labels to make further filtering and lookups easy.