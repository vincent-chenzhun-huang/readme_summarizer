an api record will contain an endpoint url a schema chosen from the available schemas provided by the additional modules as described above a mapping of fields defined in that schema to drupal tokens usually referencing fields from a node optionally one or more arguments passed through the url to filter the result set usage installation enable the main open data schema map module as usual and additionally enable any schema modules you will need to create your api.