forum announcement httpsforums.livecode.comviewtopic.phpf77t31079 script index scripttracker.md github tracking setup create a repository on github for the project clone the empty repository to the development computer configure a .gitignore file to exclude these folders move the latest version of the stack into the new repository open the stack in livecode open scripttracker select the stack in scripttracker click the select stack menu optional adjust the folder names on the stack tab of prefs optional open the log window to see results of the exports click the sync script button to do the initial script export stage and commit all of the files push changes to the remote github repository scripttracker can be configured to perform a sync every time the stack is saved.