for example if one of the dependencies of the package was not properly specified the first time you build a package then when you fix the dependency and rebuild the package you should increase the build number.