indexer can run in two modes 1 the default mode which simply queries the elasticsearch database finds the most recently modified contact then on a schedule queries the table on the rapidpro database for contacts to add or delete.